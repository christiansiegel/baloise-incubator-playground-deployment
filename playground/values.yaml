route:
  enabled: true
  host: cs-hello-world.apps.baloise.dev

backend:
  replicaCount: 2
  appPort: 8080
  image:
    repository: docker.io/christiansiegel/hello-world
    # some random comment
    tag: 1.0.0
    pullPolicy: Always
  service:
    type: ClusterIP
    port: 80
  route:
    enabled: true
    path: /
  nodeSelector: {}
  tolerations: []
  affinity: {}

kaptain:
  host: cs-kaptain.apps.baloise.dev
  replicaCount: 1
  appPort: 8080
  image:
    repository: docker.io/christiansiegel/kaptain
    tag: latest
    pullPolicy: Always
  service:
    type: ClusterIP
    port: 80
  route:
    enabled: true
    path: /
  nodeSelector: {}
  tolerations: []
  affinity: {}
  test:
    # some comment
    john: # some comment
      # some comment
      foo: bar # some comment
      doe: xx # some comment
